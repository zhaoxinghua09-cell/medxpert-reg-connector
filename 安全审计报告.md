# 安全审计报告 · medxpert-reg-connector v1.0.0

- **审计对象**：medxpert-reg-connector 发布包（reg_connector_server.py + references/ + SKILL.md + LICENSE.md + mcp-config.json + 权属证据包）
- **审计方式**：静态人工审查（本机 `skills-security-check` 技能仅含 SKILL.md、无独立扫描脚本；按云鼎流程等价检查项逐项自评）
- **审计日期**：2026-08-30
- **结论**：**P2（低风险 / benign）** —— 纯本地只读检索工具，无攻击面

## 检查项

| # | 维度 | 结果 | 说明 |
|---|---|---|---|
| 1 | 网络外发 | ✅ 无 | 脚本仅本地读取 `references/*.md`，无任何 `http`/`requests`/`socket`/subprocess 调用 |
| 2 | 凭据访问 | ✅ 无 | 零 API Key / token / 密码读取；仅可选环境变量 `REG_HUB_REFS` 指向知识库目录（用户可控） |
| 3 | 持久化 | ✅ 无 | 不写文件、不改系统、不注册服务 |
| 4 | 隐藏权限 | ✅ 无 | 无提权、无后台进程、无自启动 |
| 5 | 依赖 | ✅ 可信 | 仅 `fastmcp`（官方 PyPI 包）；无未知第三方依赖 |
| 6 | 敏感信息 | ✅ 0 命中 | `pre_publish_scan` 已验证（品牌词 MedXpert/美达信 已豁免，无真 PII/密钥） |
| 7 | 数据收集 | ✅ 无 | 不收集、不上传任何用户/客户数据 |

## 综合判定

该包为**纯本地只读 MCP 检索工具**：输入关键词 → 在本地法规知识库做 BM25 近似检索 → 返回片段与官方链接。全程不触网、不持凭据、不复写磁盘，攻击面为空。符合 P2 安全级，可发布。

> 注：返回内容为公开法规知识库线索，非官方结论；分类与注册路径须以官方分类库（NMPA/FDA/MDR）为准——属功能边界声明，非安全风险。
